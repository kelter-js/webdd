import { Typography, styled } from "@mui/material";

export const ContinueButton = styled(Typography)(() => ({
  borderBottom: "none",
  borderTop: "none",
  fontSize: "18px",
  textAlign: "center",
}));
